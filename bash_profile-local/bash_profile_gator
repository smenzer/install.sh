##
# include .bash_common 
if [ -f ~/.bash_common ]; then
  source ~/.bash_common
fi

##
# start agent and add identity
#ssh-agent && ssh-add

# User specific environment and startup programs
PATH=$PATH:$HOME/bin
export PATH
PATH=$PATH:/usr/sbin
export PATH

