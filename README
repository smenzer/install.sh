configuration for terminal including vim, screen, and tmux settings

to install:
	- in this directory, create a bash profile for the server you're on, call it .bash_profile_[SERVER_NAME]
	- from this same directory, run: bash setup [SERVER_NAME]

